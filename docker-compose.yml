version: '3.8'

services:
  gov-browser-simulator:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # Permite acesso à memória compartilhada maior para o Chrome não travar
    shm_size: '2gb'
